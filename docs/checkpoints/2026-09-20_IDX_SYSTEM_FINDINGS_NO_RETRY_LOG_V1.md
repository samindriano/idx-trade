# IDX System Findings and No-Retry Log V1

Date: 2026-09-20  
Lane: isolated `codex/alpha-available-data-20260919`

This is a durable system-research log. “No retry” means do not repeat the same probe expecting a different result without changing the contract, source, or evidence class. It does not prohibit an authorized isolated remediation test later.

| ID | Finding | Evidence | Why it matters | No-retry conclusion / reopen condition |
|---|---|---|---|---|
| SYS-EXEC-001 | Positive partial buy is not persisted as pending. | EOD close 1,000 → planned 5,000; Open 2,000 → filled 2,500; pending empty; next session no buy intent; reload preserves 2,500. | Silent underexposure and lost residual obligation across sessions. | Do not rerun the same gap case as if it were unresolved. Reopen only after quantity-aware obligation semantics change. |
| SYS-EXEC-002 | Positive partial-buy loss is trigger-general, including resolved paired replacement. | Four synthetic rows: capacity/Open change 5,000→1,200; fee boundary 1,000→900; stamp-threshold boundary 10,000→9,900; paired replacement 5,000→1,200. All had empty pending buys, equal target membership, equal cold-reload hashes, and no next-session retry. | The defect is a sizing→execution→pending→Decision contract split, not a single market-gap edge case; cost and replacement paths can silently underdeliver while seat count, NAV/cash arithmetic, and hashes remain internally valid. | Do not repeat the four trigger shapes as separate bugs. Reopen only with a different state owner, quantity-aware artifact, multi-session accounting evidence, or authorized remediation. |
| SYS-DEC-001 | Decision V2 ten-seat shadow treats a partially filled seat as fully satisfied. | Ten target names planned at 5,000 each; `T01` filled 2,400 due capacity, nine names filled 5,000; next plan reports `capacity_state=FULL`, `unfilled_slots=0`, zero effective buys and zero sizing entries. | Seat count and shadow ticker membership can suppress residual repair and make exposure/concentration appear complete. | Do not rerun another ten-seat variant without changing the quantity contract. Reopen with quantity-aware shadow state and residual-aware seat capacity. |
| SYS-CA-001 | Projected CA sizing state is incompatible with raw execution parent hash when payment settles on decision date. | Raw cash 1,000,000 vs projected 1,005,000; execution fails `EXECUTION_V1_STATE_HASH_MISMATCH`. | Valid dividend timing case is blocked before fills. | Do not retry live execution. Reopen with an explicit settle-before-prepare or projected-state hash policy. |
| SYS-CA-002 | CA lifecycle follows actual underfilled shares and cannot recover the missing residual exposure. | Synthetic 5,000 planned/1,200 actual shares produced 1,200-share entitlement, IDR 30,000 receivable, exactly-once IDR 30,000 payment, and IDR 95,000 less dividend than the planned-share hypothetical. | CA accounting remains deterministic, but upstream residual loss propagates into entitlement, receivable, payment, and total-return interpretation. | Do not classify this as a CA double-pay or source defect. Reopen only with an explicit residual-obligation model composed with CA timing. |
| SYS-ID-001 | `replacement_peer` alias can block a buy after canonical sell full-fills. | Sell `AAA` full-fills; buy peer `AAA.JK` remains pending. | Replacement liveness and portfolio transition are distorted. | Do not retry with provider aliases. Reopen only with shared identity normalization and permutation tests. |
| SYS-ID-002 | Universe normalization does not enforce uniqueness after alias normalization. | Exact runtime probe with raw keys `ABCD` and `ABCD.JK` produced two selected `ABCD` rows with liquidity ranks 1 and 2; later V4-X1 Decision adapter rejected the same duplicate frame with `DECISION_V2_V4_X1_DUPLICATE_TICKER`. | Identity protection exists late, so row-oriented universe/ranking consumers can see duplicated issuer support before the downstream guard. | Do not infer the late Decision rejection closes the universe defect. Reopen only with an authorized origin-level uniqueness contract and cross-consumer fixtures. |
| SYS-STATE-001 | Fractional shares are silently truncated by execution state normalization/reload. | `100.9` accepted as `100`; whole-lot check then passes. | State quantity can change without an explicit schema error. | Do not treat hash equality after reload as quantity correctness. Reopen with pre-cast integer validation. |
| SYS-STATE-002 | Runtime snapshot faithfully persists an underfilled positive position without a residual obligation. | Temporary runtime snapshot reload preserved `AAA:1,200`, empty pending buys, and an equal recomputed runtime hash after the 5,000-share plan underfill. | Restart integrity can be green while restart semantics preserve economic underexposure. | Do not treat snapshot/hash equality as proof of target-quantity completion. Reopen only with a quantity-aware obligation in the snapshot schema. |
| SYS-REC-001 | Staged orchestration recovery deterministically replays the underfilled state rather than repairing it. | Full `prepare_post_eod`/`execute_preopen` probe: `T00` planned 5,000, filled 2,400; after execution and snapshot deletion, `RECOVERED_STAGED_EXECUTION` returned identical execution/snapshot hashes, `T00:2,400`, and empty pending buys. | Atomic/idempotent recovery protects artifact consistency but can preserve economic underexposure across an interrupted operation. | Do not repeat recovery deletion variants without a quantity-aware transaction/state contract. Reopen only after residual obligations are explicit and recovery-aware. |
| SYS-HIST-001 | Entry and exit partial-fill semantics diverged historically. | `e1531b3c` added zero-lot buy pending and ticker-set invariants; `d8d34b79` added partial-sell pending; `ce91d60a` replay oracle retained planned/filled buy differences with pending count zero. | Historical acceptance criteria made positive entry underfill look complete while treating exit underfill as recoverable. | Do not infer intended buy semantics from the later partial-sell fix. Reopen with one explicit quantity-obligation contract for both sides. |
| SYS-TEST-001 | Existing green tests do not assert positive buy completion semantics. | Capacity test checks only gross <= cap; random test checks cash/lot/cap; allocator test accepts empty pending; replay oracle asserts pending count zero despite planned/filled differences. | Mechanical safety and deterministic replay can be mistaken for economic fulfillment, allowing the cross-component defect to pass regression gates. | Do not use the current focused green suite as closure evidence for residual semantics. Reopen with explicit planned/filled/remaining assertions. |
| SYS-EXEC-003 | Positive/partial execution obligations have no explicit remaining quantity, age, or attempt lifecycle in pending state. | Four-session synthetic replacement: sell `AAA` 5,000→1,000→1,000→1,000→0 while pending sell/buy persisted; `PendingPaperIntent` fields were only side/ticker/rank/reason/replacement peer. | Capacity starvation can persist without a typed expiry/escalation/manual-review state, and remaining quantity is reconstructed rather than state-owned. | Do not repeat the same persistence sequence. Reopen with a quantity-aware obligation ledger and explicit lifecycle events. |
| SYS-CA-003 | CA payment does not automatically re-evaluate a pending replacement. | One receivable for 5,000 shares settled IDR125,000 exactly once; cash rose from IDR47,989,507.50 to IDR48,114,507.50 while pending `AAA` sell + `BBB` buy remained unchanged. | Cash availability and execution-obligation resolution are separate state machines; operators could mistake settlement for completion or retry. | Do not classify as CA defect. Reopen only with an explicit settlement-to-replan policy and parent-state identity. |
| SYS-EXEC-004 | Decision reversal is deterministic but emits no explicit cancellation lineage for a pending pair. | Decision V2 adapter recognized shadow `BBB` → target `AAA`, produced zero effective buys/sells and zero fills; final state was `AAA:2,000` with no pending rows and `reconciliation_required=false`. | Intent reversal may be correct, but audit/reconciliation cannot identify the canceled obligation or prove pending-age escalation from the resulting state alone. | Do not infer cancellation lineage from empty pending state. Reopen with typed cancel/reversal events and audit lineage. |
| SYS-OBL-001 | The proposed quantity-obligation contract is internally replayable, but current runtime adoption is unproven. | Isolated spec harness passed 3/3: 2,400 partial fill, JSON reload, duplicate fill replay, 1,000 retry, 3,400-share CA entitlement/IDR85,000 payment, explicit 1,600 cancellation, and duplicate cancel replay. | A coherent design can still diverge from the production state owner; spec PASS must not be mistaken for runtime remediation. | Do not call the runtime fixed. Reopen with a separately authorized implementation/replay lane, including SELL/replacement and migration fixtures. |
| SYS-OBL-002 | The proposed obligation graph preserves paired SELL/BUY replacement semantics under retry and reversal. | Independent 2/2 harness: `AAA:5,000` → partial SELL 1,000 → retry SELL 1,000 → `AAA:3,000`; paired BUY stayed blocked; JSON reload hash matched; reversal canceled SELL remaining 3,000 and BUY remaining 5,000 with six event IDs retained. | A single quantity/event contract can represent the historical partial-SELL behavior and the missing BUY residual without ticker-only state. | Do not infer runtime remediation from the spec pass. Reopen with artifact/schema implementation and real-runtime compatibility tests. |
| SYS-ART-OBL-001 | Prepared sizing and completed fills are serialized, but durable state lacks a logical obligation join. | `_execution_plan_payload` preserves planned sizing; completed execution preserves `planned_shares`/`filled_shares`; `_snapshot_payload` persists only positions, ticker-level pending rows, CA ledger/registry, and hashes. No obligation ID, remaining quantity, attempt history, or cancellation event is serialized. | Hash-connected artifacts can remain internally consistent while the reloadable state loses the planned-to-filled-to-remaining relationship. | Do not treat existing artifact hashes as obligation completeness. Reopen with versioned snapshot/fill schema and legacy orphan classification. |
| SYS-ART-OBL-002 | Legacy obligation migration is evidence-dependent and must remain fail-closed. | Isolated 6-case harness classifies complete fill, explicit zero-lot pending, recoverable positive partial with preserved fill vector, snapshot-only `UNKNOWN_ORPHANED_PARTIAL`, and quantity inversion requiring reconciliation. | A compatibility migration can safely recover only what the retained execution vector proves; snapshot position alone cannot establish planned or remaining quantity. | Do not implement inferred residuals. Reopen with an authorized versioned migration/replay implementation and real artifact fixtures. |
| SYS-CA-REP-001 | Malformed optional split/dividend values become apparent no-events. | `"2:1"`/`"bad"` → `NaN → fillna(0)`; CA flags false. | CA completeness can be overstated. | Do not retry fallback acquisition from this lane. Reopen with invalid/unknown CA schema policy. |
| SYS-STORAGE-001 | Duplicate dates have asymmetric behavior. | Existing duplicates raise ambiguous-Series `ValueError`; incoming duplicates silently keep last. | Revision lineage and data integrity are not uniform. | Do not rerun duplicate probes without source contract change. Reopen with typed duplicate rejection/provenance. |
| SYS-EVAL-001 | Pure evaluator and final gate use different ticker identity normalization. | Pure metrics accept `ALIS`/`ALIS.JK`; gate rejects normalized collision; 120 tests pass. | Research measurements can differ from admission behavior. | Do not interpret green pure metrics as gate-compatible. Reopen with one shared identity function. |
| SYS-UNIVERSE-001 | Universe warmup/era identity boundary is unproven. | Pre-listing rows count toward warmup; aliases and overlapping eras accepted in synthetic probes. | Eligibility and historical identity can distort downstream ranks. | Do not patch canonical universe from this lane. Reopen with explicit era/identity policy. |

## Negative results worth retaining

- Existing focused tests are valuable regression evidence but are not system closure evidence.
- Hash chains can preserve a semantically incomplete state; internal integrity is not equivalent to economic completeness.
- Fail-closed behavior can still be operationally wrong when the rejected state was the only valid path, as in payment-on-decision-date sizing.
- The current defects were reproducible without protected outcomes or new provider data.
- The orchestration serializer does preserve per-entry planned sizing inside
  `execution_plan.sizing_plan.entries` (ticker, lots, shares, notional, and
  status). No artifact-omission defect was admitted from the serializer probe;
  the remaining gap is the missing planned-to-filled-to-remaining linkage in
  execution state and pending obligations.
