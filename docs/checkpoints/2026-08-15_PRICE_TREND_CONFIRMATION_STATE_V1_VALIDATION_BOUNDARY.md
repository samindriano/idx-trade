# Validation boundary

Scoped CI may execute only the deterministic state tests. Repository-wide local
pytest remains a review requirement. No provider, scheduler, runtime capture,
O2, Foreign Flow, model, or outcome action is authorized by validation.
