# Project continuity master created

Date: 2026-08-09

A comprehensive cross-chat bootstrap document now exists at:

- `docs/PROJECT_CONTEXT_MASTER.md`

Purpose:

- allow a new ChatGPT/Codex session to recover the project without depending on
  the previous conversation context;
- preserve project objective, stage map, validation philosophy, source hierarchy,
  ontology, architecture, branch/PR map, certified hashes, failure chronology,
  active blockers, and next-action contracts in one document;
- complement rather than replace `docs/PROJECT_LEDGER.md`.

`AGENTS.md` now requires material DATA/VALIDATION/MODEL work to read, in order:

1. `docs/PROJECT_CONTEXT_MASTER.md`;
2. `docs/PROJECT_LEDGER.md`;
3. the newest relevant file under `docs/checkpoints/`;
4. then verify the actual branch and HEAD before acting.

The master context intentionally records both proven facts and explicitly marked
proposed-next-step contracts. Proposed or planned work must never be mistaken for
runtime PASS evidence.

At creation time, the latest proven project state remains:

- 43-session full market: certified PASS;
- 126-session full market: certified PASS;
- 504-session history: not certified; latest documented repair stopped on
  FREN/MASA/MFIN historical price/opening-price evidence;
- 1260-session history: preparation branch only, not executed/certified;
- modelling and `IDX-VAL-002`: not started;
- merge to `main`: not authorized.

Future material checkpoints must update the master context and chronological
ledger so context loss never requires reconstructing the project from chat
history alone.
