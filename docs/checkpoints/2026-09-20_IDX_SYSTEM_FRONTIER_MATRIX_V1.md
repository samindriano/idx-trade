# IDX System Frontier Matrix V1

Date: 2026-09-20  
Lane: isolated `codex/alpha-available-data-20260919`  
Purpose: durable rolling map for the system-wide research marathon

Depth labels are evidence states, not promotion claims. `EXTERNAL_EVIDENCE_BLOCKED` means local work is bounded by protected outcomes, provider ownership, or live production evidence and is not a claim of correctness.

| Surface | Depth | Current evidence / finding | Open local question or blocker | Next useful frontier |
|---|---|---|---|---|
| Alpha / representation interface | STRUCTURALLY_AUDITED | Pure evaluator/gate ticker identity split; outcome evidence remains closed. | Test representation-to-Decision churn without protected targets. | Adversarial alpha-to-Decision interface replay. |
| Decision state machine | CROSS_COMPONENT_TESTED | Rank persistence, exit confirmation, vacancy, soft replacement, and Decision V2 ten-seat shadow scenarios exercised; a 2,400/5,000 seat still reports `FULL`. | Quantity-aware paper state is not represented in Decision membership. | Connect Decision state to residual execution obligations. |
| Portfolio construction / sizing | CROSS_COMPONENT_TESTED | EOD-close sizing can plan 5,000 shares while Open execution fills 2,500; capacity, fee, stamp-boundary, and paired-replacement variants all underfill positively. | Positive partial fills are not a sizing-state contract. | Quantity-aware target/residual model. |
| Turnover / holding-state behavior | STRUCTURALLY_AUDITED | Turnover and holding-duration metrics exist; pending/open denominator contracts tested. | Partial positive buys can distort holding and turnover interpretation. | Recompute metrics from residual-aware fills. |
| Execution / pending / replacement | CROSS_COMPONENT_TESTED | Partial sells persist across repeated capacity attempts; a paired sell/buy remains pending through zero capacity and CA payment, while positive partial buys disappear across four trigger classes. A target reversal drops the pending pair without a fill-based cancellation record. | Residual quantity, age/attempt, expiry/escalation, and explicit cancellation semantics. | Unified order-obligation ledger with lifecycle events. |
| Transaction-cost assumptions | CROSS_COMPONENT_TESTED | Fee/slippage/stamp rules are explicit and allocator-aware, but fee and stamp-boundary underfills are still recorded as complete positive fills. | Positive partial buy under cost pressure can be misclassified as complete. | Cost-aware residual obligation tests. |
| Liquidity / capacity | CROSS_COMPONENT_TESTED | Reference-day capacity and Open allocation interact with sizing; positive partial buys reproduce under capacity and paired replacement. | Capacity-limited buy residual semantics. | Capacity stress matrix with partial buy/sell symmetry. |
| Portfolio accounting / NAV / cash | CROSS_COMPONENT_TESTED | Receivable contributes to NAV but not spendable cash; payment-on-decision sizing hash mismatch found; a synthetic IDR125,000 settlement changes cash while a pending replacement remains unchanged. The runtime `reconciliation_required` bit is serialized and gated but not produced by a mismatch detector. | Settlement-to-replan policy, residual cash taxonomy, and a real reconciliation evidence contract. | Three timing cases plus explicit pending/settlement policy and versioned reconciliation source. |
| Exposure / cash-state taxonomy | STRUCTURALLY_AUDITED | Decision artifacts retain `capacity_state`/`unfilled_slots`, but restartable `PaperState` and Decision shadow reconstruction retain only cash, positions, pending tickers, and the prior reconciliation bit. Distinct no-challenger and capacity-limited histories can have the same state hash. | Cause taxonomy, obligation join, risk-hold semantics, and restart/replan policy are not state-owned. | Versioned exposure/cash-state contract composed with quantity obligations and CA settlement. |
| Dividends / CA integration | CROSS_COMPONENT_TESTED | Entitlement/receivable/payment lifecycle and restart tested; projected-vs-raw state incompatibility found; actual underfilled shares correctly drive CA; a multi-session payment remains exactly-once without automatically retrying the pending replacement. | Noncash CA remains fail-closed/external-blocked; residual-obligation/CA timing and replan policy are not certified. | CA lifecycle into residual-aware execution and settlement policy. |
| Risk / concentration / exposure | CROSS_COMPONENT_TESTED | Seat cap, per-name cap, liquidity and concentration audits exist; positive underfill can satisfy ticker membership while missing target quantity. | Concentration and exposure need target-vs-filled obligations. | Risk metrics with residual-aware quantities. |
| Security / ticker / issuer identity | CROSS_COMPONENT_TESTED | Alias collisions found in universe, evaluator, and execution relationship fields. Exact runtime probes show `ABCD` + `ABCD.JK` become two selected `ABCD` universe rows; same-class security-master revisions are order-sensitive, while the documented active/delisted preference is intentional; Decision later rejects the alias duplicate frame. | Shared canonical identity and same-class revision contract is not enforced at every boundary. | Cross-layer identity/revision regression matrix with origin-level uniqueness, conflict typing, and downstream containment. |
| Universe / eligibility interaction | ADVERSARIALLY_TESTED | Pre-listing warmup, alias collisions, overlapping eras reproduced. Universe alias collision is deterministic and row-duplicating before the later Decision guard. | Canonical identity/era policy remains unimplemented at the universe origin. | Link universe uniqueness and era identity to Decision/execution state. |
| Storage / revisions / duplicates | ADVERSARIALLY_TESTED | Existing duplicate dates can crash; incoming duplicates keep last; atomic writers replace destinations. | Typed duplicate/revision policy. | Persistence contract and immutable/mutable writer split. |
| Artifact integrity | CROSS_COMPONENT_TESTED | Hash chains and exclusive prospective gate outputs tested; prepared artifacts preserve planned sizing and completed artifacts preserve fill vectors. The durable snapshot omits a logical obligation join, remaining quantities, and cancellation lineage. A 6-case migration harness now classifies synthetic legacy shapes without fabricating residuals; the pinned runtime checkout contains no retained fill-vector fixture. The reconciliation bit is carried in hashes/snapshots but is not independently evidenced. | Versioned obligation/fill/snapshot schema, authorized real-fixture adoption, and explicit reconciliation evidence. | Bind quantity obligations and reconciliation provenance into state/artifact lineage without rewriting historical oracle; preserve `UNKNOWN_ORPHANED_PARTIAL`. |
| Runtime configuration lineage | STRUCTURALLY_AUDITED | Current HEAD revalidation confirms the loader carries config/runner hashes while prepared/execution orchestration artifacts still omit both fields. | Full executable/config identity binding and fail-closed equality at stage handoff/replay. | Add a separately authorized versioned lineage contract; no runtime adoption in this lane. |
| Restart / reload / idempotency | CROSS_COMPONENT_TESTED | 59 runtime tests and CA chain reload pass; actual runtime snapshot reload preserves the wrong positive partial position with equal hash and empty pending state. Cause taxonomy is also absent from the state used to reconstruct the next Decision. | Idempotency can preserve semantically incomplete quantity and causal state. | Replay with residual obligations plus versioned cause/state lineage. |
| Failure recovery / interrupted state | CROSS_COMPONENT_TESTED | Interrupted atomic states, missed Open, and full staged recovery exercised; recovery preserves the positive underfill with equal hashes and empty pending state. | Recovery does not reconstruct missing positive buy residual. | Fault injection after an explicit residual obligation is modeled. |
| Evaluation contracts | CROSS_COMPONENT_TESTED | 120 prospective evaluator/gate/readiness tests pass; pure/gate identity mismatch found. | Protected outcomes remain closed. | Outcome-blind evaluator/runtime contract audit. |
| Research methodology / scientific controls | STRUCTURALLY_AUDITED | PIT, target firewall, OOS and evidence boundaries documented. | No protected predictive validation authorized. | Audit structural metrics versus economic interpretation. |
| Historical design archaeology | CROSS_COMPONENT_TESTED | `e1531b3c` introduced zero-lot BUY pending and ticker membership invariants; `d8d34b79` later added positive partial-SELL persistence; `ce91d60a` replayed planned/filled BUY differences while expecting zero pending. Compatibility and migration boundaries are now documented. | Existing positive partial BUY state cannot recover lost remainder; legacy ticker-only pending lacks quantity/age. | Versioned obligation/replay harness with explicit orphan handling. |
| Cross-component interfaces | CROSS_COMPONENT_TESTED | CA→sizing→execution hash mismatch; sizing→execution positive partial loss across four trigger classes; repeated partial exit plus CA payment preserves pending state; Decision reversal can remove pending rows without explicit cancellation; identity→replacement mismatch; Decision capacity/cash cause is not joined to restartable state. | Need a single obligation/state graph, cause taxonomy, and settlement/reversal policy. | Multi-session obligation ledger with lifecycle/audit events and state-owned exposure causes. |
| Quantity-obligation state graph | STRUCTURALLY_AUDITED | Evidence-driven V1 proposal defines planned/filled/remaining/relinquished quantities, lifecycle events, CA settlement policy, reversal lineage, restart identity, and risk projections; BUY and SELL/replacement JSON-replay spec harnesses pass 3/3 and 2/2; migration audit passes 6/6 with snapshot-only partials left unknown. The pinned runtime has no retained fixture for real migration, and the graph is not implemented in runtime. | Authorized real legacy fixture, event-level replay compatibility, and runtime adoption. | Compare event-level acceptance against actual artifacts in an authorized implementation lane; no production adoption or inferred migration. |

## Current matrix interpretation

The system is no longer best described as “component tests mostly pass with a few isolated gaps.” The highest-risk pattern is semantic state loss at boundaries: a component can produce an internally valid artifact whose meaning is incomplete for the next component. The positive partial-buy trigger matrix shows that this is a family-level execution contract failure, not a single gap scenario. The most valuable next work is quantity-aware state modeling and a multi-session residual-order harness, not additional alpha feature search.

## External/protected boundaries

- Protected H5/H10, hidden OOS/PnL, and incumbent predictive comparisons remain closed.
- New external data acquisition belongs to another lane and was not used here.
- Live cloud/capture/telemetry/scheduler behavior remains out of scope for mutation.
