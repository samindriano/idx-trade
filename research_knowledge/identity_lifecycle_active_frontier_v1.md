# Active Frontier — Population Identity/Lifecycle Authority

Last checkpoint: 2026-09-20  
Branch: `codex/idx-population-identity-lifecycle-20260920`  
Status: ACTIVE, evidence-only, admission blocked.

## Completed in this lane

- Enumerated a 1,176-code normalized observed research-population union from 17 retained
  source families with exact source membership and pairwise differences.
- Attempted all 984 codes in the retained KSEI registered-share table once and
  preserved every raw response and acquisition metadata record.
- Parsed 371 internally valid KSEI detail pages; retained 2 HTTP-200 invalid
  pages and 611 HTTP-500 outcomes as unresolved.
- Crosswalked current listings, delisting rows, security-master intervals,
  issued-history events, profiles, reports, financial bundle, foreign flow,
  sector, panel, anchors, and public mirrors.
- Materialized a typed, provenance-bearing identity/lifecycle graph and a
  per-code residual table.
- Completed the final retained-source counterexample census: two profile-name
  multi-code groups and one KSEI issuer-string multi-security group were found;
  the crosswalk preserves separate ticker/security/ISIN nodes.
- Integrated the retained corporate-action document probe (three codes, eight
  document-evidence records) as event/document anatomy only; no effective
  first-session or price-basis authority was inferred.
- Audited all six retained lifecycle conflicts against exact announcement
  codes, current/delisting intervals, issued-history types, profiles, and KSEI
  status. The classes are heterogeneous; only two SKBM subsidiary-name notices
  were found, and no interval winner was selected.
- Crosswalked all 115 official-delisting-only codes against every retained
  announcement-search result file. There were zero exact hits; this is recorded
  as bounded no-hit-not-negative evidence, not event absence.
- Confirmed panel/trading/anchor counterexamples, including CNTX and CNTB.
- Confirmed 115 codes are present only in the retained official delisting-record
  source and 3 valid ticker codes only in the public IPO cross-check; five
  hyphenated canceled-offering identifiers remain outside the normalized union.
  These are residual population leads, not automatically admitted securities.

## Highest-information next questions

1. Identify whether the 133 observed codes outside the retained profile-file
   union represent historical-only, non-equity, stale-code, or missing-profile
   classes. Start with issued-history and delisting evidence; do not infer from
   first/last trade.
2. Separate the 197 observed codes outside the current security master into
   historical-only, KSEI-only, public-mirror-only, and likely invalid/non-stock
   classes. Preserve every class as UNKNOWN until a distinct identity source
   resolves it.
3. Attack ticker reuse/relisting and share-series collisions as classes using
   official announcement/issuer/KSEI history, beginning with the six retained
   conflict codes and CNTX/CNTB. Do not generalize one resolved edge.
4. Examine KSEI historical announcement/ISIN surfaces for whether they enumerate
   security events or merely provide search-result discovery. Record coverage,
   time, identity, and effective-date limitations.
5. Test the final counterexample set for historical absent nodes, incorrect
   collapses, ticker reuse, series collisions, relisting mistakes, and issuer
   continuity mistakes. The retained-source pass now has explicit name/security
   collision counterexamples; historical reuse/relisting/continuity still
   require a versioned external identity source. A new case reopens the lane
   rather than being repaired silently.

## Hard boundaries

No protected predictive outcomes, H5/H10/OOS/PnL, model fitting, canonical
overwrite, provider call, production/cloud/capture/telemetry/scheduler mutation,
push, merge, or fabricated authority. Current artifacts are structural and
source-authority evidence only.

## Reopen conditions

The blocked PIT/population question reopens only on a new versioned enumerating
source or explicit owner policy. Same-family snapshot retries do not change the
authority class. A candidate remains inadmissible while population, identity,
available-at/revision, and corporate-action basis contracts are incomplete.
