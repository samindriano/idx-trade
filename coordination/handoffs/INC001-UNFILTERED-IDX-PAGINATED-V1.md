# Handoff: INC-001 V1.1 complete IDX unfiltered pagination

Task: `INC001-CA-UNFILTERED-IDX-PAGINATION-V11`  
Branch: `data/ca-aware-feature-basis-remediation-v1`

## Decision

`NO-GO` for any further provider acquisition or scientific execution. The
exact unfiltered IDX result was completed across the two authorized additional
pages and reconciled locally. Stop for ChatGPT review.

## Controlling evidence

Artifact root: `D:\Documents\Project\idx-ca-source-authority-unfiltered-probe-20260829-v1`

The page 1, page 2, and page 3 response bytes, exact URLs/parameters,
timestamps, HTTP/result metadata, hashes, and every returned row are retained.
The full union is 700 unique source IDs with no duplicates, invalid IDs, or
cross-page payload conflicts.

The category result remains semantically incomplete: 498/700 broad rows are
missing from the already captured nine-category union, including 290
structural/action candidates. Exact source-native labels and row identities
are preserved; unsupported labels remain taxonomy-only or forensic/outside
scope and are not force-mapped.

The six retained-only identities are now fully comparable: five present and
identical; one (`82860 PACK`) absent. Set
`RETAINED_ONLY_TRUE_DISAPPEARANCE_COUNT=1` and
`IDX_HISTORICAL_SNAPSHOT_MONOTONICITY=FAIL_OBSERVED_TRUE_DISAPPEARANCE`.
This does not provide historical as-of or no-event authority.

The accepted physical census remains 412 (121 resolved, 291 unresolved); no
new in-scope physical event is admitted. Existing family/source unresolved
counts remain unchanged. Candidate dates are not transition dates.

## Boundaries

No further IDX query, page-1 refetch, category refetch, KSEI or schedule
request, taxonomy expansion, Phase-E, outcome/target access, model
fit/refit/scoring, counter mutation, canonical historical rewrite, or PR
#103/#108 merge is authorized.

